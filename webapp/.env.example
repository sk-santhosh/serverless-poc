# Address of the Redis instance backing the welcome-email queue.
REDIS_ADDRESS=localhost:6379

# Must match the operator chart's queueWorkers[].redis.stream for the
# corresponding QueueWorker CR. See deploy/helm/README.md for details.
REDIS_STREAM=welcome-email-queue
